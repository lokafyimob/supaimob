{
  "buildCommand": "prisma generate && SKIP_ENV_VALIDATION=1 npx next build --no-lint",
  "framework": "nextjs",
  "installCommand": "npm install",
  "env": {
    "SKIP_ENV_VALIDATION": "1",
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}